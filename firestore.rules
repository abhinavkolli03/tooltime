rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules are PERMISSIVE for demo/MVP purposes only.
      // They allow any authenticated user to read and write any document.
      allow read, write: if request.auth != null;
    }
  }
}
